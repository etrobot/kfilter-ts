{
  "name": "filter-ts",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:native": "pnpm --filter native dev",
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "db:push": "pnpm --filter @filter-ts/db db:push",
    "db:studio": "pnpm --filter @filter-ts/db db:studio",
    "db:generate": "pnpm --filter @filter-ts/db db:generate",
    "db:migrate": "pnpm --filter @filter-ts/db db:migrate",
    "db:local": "pnpm --filter @filter-ts/db db:local"
  },
  "dependencies": {
    "@filter-ts/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@filter-ts/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.14.0"
}
